{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,

    // IMPORTANT: strict must be OFF for Next.js route handlers
    "strict": false,

    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "incremental": true,
    "jsx": "preserve",

    "types": ["node", "react", "react-dom"],

    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    },

    "typeRoots": [
      "./types",               // 👈 added
      "./node_modules/@types"
    ],

    "plugins": [
      {
        "name": "next"
      }
    ]
  },

  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "types/**/*.d.ts",
    "lib/**/*.ts",         // 👈 added
    "models/User.ts",
    "middleware.ts",
    "app/api/**/*"
, "app/website/pagetsx", "app/admin/sellers/create"  ],

  "exclude": ["node_modules"]
}
